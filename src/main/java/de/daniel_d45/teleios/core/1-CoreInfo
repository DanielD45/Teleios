- Plugin name:

greek: Téleios (Τέλειος) = english: complete
pronunciation: pronounced like TELL-e-os


- Features:

1 setdebuglevel command
1 manageteleios command
1 activationstate enum
1 remove all permission checks
1 new DebugLevel system:
    1 add info messages
    1 odd = terminal output, even = terminal and chat output
------------------------------------------------------------------------------------------------------------------------------------------------------
WIP:
0 upgrade to 1.20 -> wait for the spigot 1.20 stable version
0 fix: assigning mode exit message coupled with function not active message
0 PLCs
    0 personal invs: Bug: ConfigEditor.set();
    0 barrels... too
    0 restock PLCs
0 listener event priorities
0 ConfigEditor: change initiate wpp...
0 ConfigEditor: get & save previous value, get new value & test it, set new value, query value from config & test it, (if value invalid: recover previous value & notify console)
0 fill SampleCode class
------------------------------------------------------------------------------------------------------------------------------------------------------
0 make more specialised listeners?
0 MTL inventory: activated segments have enchanted icons
0 MTL inventory: icons do not show enchantments or damage specs
0 command (or MTL inv) to de-implement commands to make space for other plugins' command names
0 fix: teleporter recipe being created multiple times
0 fix: remove loading time when listing a lot of teleporters -> send message as one, reduce loading time
0 fix: broadcasting doesn't work in onEnable()
0 command tablists


- Interesting classes and methods:
LivingEntity
 addPotionEffect(PotionEffect effect)


- File Naming Notes:
Cmd = Command
Lst = Listener


- Debug message colours:

red (§c)    / dark red (§4):	method failed
green (§a)  / dark green (§2):	method ran successfully
yellow (§e) / gold (§6):	    method ran without real impact / method skipped


- Chat message / command feedback colors:

green (§a) 	/ gold (§6): success message
yellow (§e) / gold (§6): info message
red (§c) 	/ gold (§6): fail message


- Valid and standard argument values:

ArgumentName (StandardValue): Range
DebugLevel (1): x >= 0; x <= 8;
                0  = no messages
                1  = plugin start/end messages will be written to the terminal
                2  = plugin start/end messages will be written to the terminal & in-game chat
                3  = plugin start/end + fail messages will be written to the terminal
                4  = plugin start/end + fail messages will be written to the terminal & in-game chat
                5  = plugin start/end + fail + warning messages will be written to the terminal
                6  = plugin start/end + fail + warning messages will be written to the terminal & in-game chat
                7  = plugin start/end + fail + warning + exit messages will be written to the terminal
                8  = plugin start/end + fail + warning + exit messages will be written to the terminal & in-game chat

Fail messages print a short message and the exception stack trace on encountered exception.
Warning messages inform the console that something unexpected happened or action is necessary.
Exit messages inform the console that and how a method finished.


- Data structure for the plugin.yml:

commands:
   [command1]:
      aliases:
      - [c1]
      permission: teleios.[segment1].[command1]
      description: [description1]
      usage: /[command1]

permissions:
   teleios.[segment1].[command1]:
      description: Allows the [command] command.
      default: true
   teleios.[segment1].[command2]Read:
      description: Allows the [command] command.
      default: true
   teleios.[segment1].[command2]:
      description: Allows the [command] command.
      default: op
